<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wantapp.netaq.com.wantapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".screens.MainActivity"
            android:launchMode="singleTop">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".screens.needs.ScreenNewWant" />
        <activity android:name=".screens.ScreenCreateProfile" />
        <activity android:name=".screens.ScreenSignIn" />
        <activity android:name=".screens.alerts.ScreenAddAlert" />
        <activity
            android:name=".screens.ScreenVerifyOTP"
            android:label="@string/title_activity_screen_verify_otp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".screens.register.ScreenRegistrationHost"
            android:theme="@style/AppTheme.NoActionBar">

             <intent-filter>
             <action android:name="android.intent.action.MAIN" />


             <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter> 
        </activity>
        <activity android:name=".screens.chat_list.ScreenChatList">

        </activity>
        <activity android:name=".screens.chat_screen.ScreenChat" />
    </application>

</manifest>